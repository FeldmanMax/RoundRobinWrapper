env = "test"

loggingDestinations="donotlog"

maxRetries=5

httpConnectionTimeoutInMillis=100
httpReadTimeoutInMillis=200

weightRate.increase {
  isPercent = false
  quantity = 10
}

weightRate.decrease {
  isPercent = false
  quantity = 20
}